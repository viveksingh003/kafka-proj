package PDC;

import javax.xml.stream.Location;

public enum EntityType {
 /*   LOCATION(getEntityNAme(LocationType.class),LocationType.class)*/
    //similar for all entities

    //amd many more code
}
